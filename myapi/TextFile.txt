start postgresql

"C:/Program Files/PostgreSQL/9.6/bin\pg_ctl" -D "c:/datadir" -l c:\logs\logfile.txt  start

to get to command line:

.\psql.exe mydb postgres
password=FireBert!


could not get dotnet ef command to work using PS instead.

Tools are split 
For PowerShell support: Microsoft.EntityFrameworkCore.Tools 1.1.0
For dotnet CLI support: Microsoft.EntityFrameworkCore.Tools.DotNet 1.0.0

//    https://docs.microsoft.com/en-us/ef/core/miscellaneous/cli/powershell

// add the configuration stuff first then;;;;
Add-Migration init

// use script migration to see a script for the migration
Script-Migration

// you must use Remove-Migration to remove a migration!  Do not just delete the .cs files
Remove-Migration

// use update database to apply changes to database
Update-Database

// reverse engineer a database using scaffold dbcontext
Scaffold-DbContext

// now we setup the controller.

test with this link   http://localhost:1479/api/weather

// now we seed the data.
// we are reading data from JSON file using the app services to get the context and pass to a seeder class
  // more info on .net core dependency injection  https://wildermuth.com/2016/08/07/ASP-NET-Core-Dependency-Injection

